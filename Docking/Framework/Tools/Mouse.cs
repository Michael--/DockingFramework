namespace Docking.Tools
{
   public static class Mouse
   {
      public const int LEFT_MOUSE_BUTTON  = 1; // sadly, GTK# does not define these numbers itself. we have to do it on our own.
      public const int RIGHT_MOUSE_BUTTON = 3; // sadly, GTK# does not define these numbers itself. we have to do it on our own.
   }
}

